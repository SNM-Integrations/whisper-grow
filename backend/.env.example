# Second Brain - Backend Configuration
# Copy this to .env and configure for your environment

# ===========================================
# MODE: "local" or "cloud"
# ===========================================
BRAIN_MODE=local

# ===========================================
# LOCAL MODE - Ollama Configuration
# ===========================================
# URL where Ollama is running
OLLAMA_URL=http://localhost:11434

# Model to use (must be installed in Ollama)
OLLAMA_MODEL=gemma3:4b

# SQLite database path (relative to backend folder)
SQLITE_PATH=brain.db

# ===========================================
# CLOUD MODE - LLM Configuration
# ===========================================
# Provider: "anthropic" or "openai"
LLM_PROVIDER=anthropic

# Anthropic (Claude) settings
ANTHROPIC_API_KEY=your-api-key-here
ANTHROPIC_MODEL=claude-sonnet-4-20250514

# OpenAI settings
OPENAI_API_KEY=your-api-key-here
OPENAI_MODEL=gpt-4o

# ===========================================
# CLOUD MODE - Database Configuration
# ===========================================
# PostgreSQL connection string
DATABASE_URL=postgresql://user:password@host:5432/database

# ===========================================
# SERVER CONFIGURATION
# ===========================================
HOST=0.0.0.0
PORT=8000

# Frontend URL (for CORS)
FRONTEND_URL=http://localhost:8080

# Additional CORS origins (comma-separated)
# CORS_ORIGINS=https://yourdomain.com,https://app.yourdomain.com
