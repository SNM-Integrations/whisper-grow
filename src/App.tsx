import React from "react";

// Minimal shell to verify tooltip/toast crash is gone. We'll restore routes after confirming.
const App = () => (
  <div className="p-6">
    <h1 className="text-lg font-semibold">App shell (diagnostic)</h1>
  </div>
);

export default App;
